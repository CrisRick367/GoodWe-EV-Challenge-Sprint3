# Copie este arquivo para ".env" e preencha os valores.
# O arquivo ".env" NUNCA deve ser enviado ao Git (já está no .gitignore).
# Cole a chave sem espaços e sem quebra de linha no final.

OPENAI_API_KEY=

# Opcional: só é usada se o projeto da OpenAI não liberar um segundo modelo para a comparação.
GEMINI_API_KEY=
